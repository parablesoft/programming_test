
{{!-- You will need to change this code! --}}
{{!-- {{outlet}} --}}

<div class="main-content">
  <p><em>You will need to move this to the admin page</em></p>
  <h1>Users</h1>
  <table>
    <thead>
      <th>First Name</th>
      <th>Last Name</th>
      <th>Token</th>
      <th>Actions</th>
    </thead>
    <tbody>
      {{#each model as |user|}}
        <tr>
          <td>{{user.firstName}}</td>
          <td>{{user.lastName}}</td>
          <td>----</td>
          <td>
            <button>Regenerate Token</button>
          </td>
        </tr>
      {{/each}}
    </tbody>
  </table>
</div>
